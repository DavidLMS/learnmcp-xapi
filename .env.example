# Environment Configuration
ENV=development

# LRS Configuration (REQUIRED)
LRS_ENDPOINT=http://localhost:8080
LRS_KEY=your-lrsql-api-key-here
LRS_SECRET=your-lrsql-api-secret-here

# Actor Configuration (REQUIRED)
# Each client (student) must set a unique UUID
ACTOR_UUID=student-12345-67890-abcdef

# Optional Configuration
RATE_LIMIT_PER_MINUTE=30
MAX_BODY_SIZE=16384
LOG_LEVEL=INFO

# Configuration Notes:
# 1. Each student should have a unique ACTOR_UUID in their Claude Desktop config
# 2. Use HTTPS for LRS_ENDPOINT in production
# 3. LRS_KEY and LRS_SECRET come from your LRS admin interface
# 4. LOG_LEVEL can be DEBUG, INFO, WARNING, ERROR